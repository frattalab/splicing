__default__:
  mem: 16000
  cpu: 4
  out: logs/{rule}_{wildcards}.log
  partition: general

build:
   partition: long
   mem: 64000
   out: logs/{rule}_{wildcards}.log

qc:
   mem: 32000

junctioseq_analysis:
   mem: 32000
   cpu: 10
   out: logs/{rule}_{wildcards}.log